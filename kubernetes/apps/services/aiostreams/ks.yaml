apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: aiostreams
  namespace: flux-system
spec:
  targetNamespace: services
  interval: 1h
  prune: true
  path: ./kubernetes/apps/services/aiostreams/base
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  postBuild:
    substituteFrom:
      - kind: Secret
        name: cluster-secrets
  wait: false
