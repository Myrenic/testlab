apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: domain-0
spec:
  secretName: domain-0-prod-tls
  issuerRef:
    name: domain-0-prod
    kind: ClusterIssuer
  dnsNames:
    - "${SECRET_DOMAIN}"